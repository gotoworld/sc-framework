@[
for(t in tcfgs){
if(!strUtil.containsStr(t.tableName,'_vs_')){
 /]
        <li ng-show='staff.hasPermission("${strUtil.toLowerCaseFirstOne(t.tableNameFormat)}:menu")' ><a class="J_menuItem" href="/html/${strUtil.getDir(t.schemaConf._view_pkg)}/${t.tableName}.html">${t.tableComment}</a></li>
@[
}}
 /]