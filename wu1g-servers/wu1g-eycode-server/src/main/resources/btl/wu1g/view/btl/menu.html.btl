<%
for(t in tcfgs){
if(!strUtil.containsStr(t.tableName,'_vs_')){
%>
    \<% if(shiro.hasPermission("${strUtil.toLowerCaseFirstOne(t.tableNameFormat)}:menu")){ %>
        <li><a class="J_menuItem" href="\${basePath }h/${t.scfg._my_pkg}/${strUtil.toLowerCaseFirstOne(t.tableNameFormat)}/init">${t.tableComment}</a></li>
    \<%}%>
<%
}}
%>