info:
  description: 配置分发
  version : v1.0
server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: classpath:/yml-config
        git:
          uri: http://192.168.101.16/wuxiaogang/hsd-servers.git
          searchPaths: doc/yml-config

security:
  user:
    name: admin
    password: admin