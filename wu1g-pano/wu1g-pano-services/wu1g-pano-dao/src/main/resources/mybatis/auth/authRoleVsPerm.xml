<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.com.baseos.dao.daointer.auth.IAuthRoleVsPermDao" >
	<!-- 字段列表 -->
	<sql id="Base_Column_List" >
		t1.role_id as "roleId",
		t1.perm_id as "permId",
		t1.date_created as "dateCreated",
		t1.create_id as "createId",
		t1.create_ip as "createIp",
		t1.version as "version"
	</sql>
	<!-- 新增 批量数据 -->
	<insert id="insertBatch">
		insert into t_auth_role_vs_perm(
		role_id,
		perm_id,
		date_created,
		create_id,
		create_ip,
		version
		) values 
		<foreach collection="list" item="t" separator=",">
		(
		#{t.roleId},
		#{t.permId},
		now(),
		#{t.createId},
		#{t.createIp},
		0
		)
		</foreach>
	</insert>
	<!-- 根据角色id物理删除信息 -->
	<delete id="deleteDataByRid" parameterType="map" >
		delete
		from t_auth_role_vs_perm
		where  role_id = #{roleId}
	</delete>
</mapper>