
                        <div class="form-group clearfix">
                            <label class="col-md-3 control-label">${t.columnComment}：</label>
                            <div class="col-md-12">
                                <input id="bean-${t.columnNameFormat}" name="${t.columnNameFormat}" value="{{item.${t.columnNameFormat}}}" class="form-control" type="text"  placeholder="请输入${t.columnComment}" @[ if(t.isNullable!='YES'){ /] required="" aria-required="true" @[ } /] aria-describedby="bean-${t.columnNameFormat}-error" @[ if(t.dataLength!=null){ /] maxlength="${t.dataLength}" @[ } /]  aria-invalid="true">
                                <label id="bean-${t.columnNameFormat}-error" class="help-block m-b-none" for="bean-${t.columnNameFormat}"></label>
                            </div>
                        </div>
