<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wu1g.dao.sys.ISysMessageDao">
    <!-- 字段列表 -->
    <sql id="Base_Column_List">
         t1.id as "id"
        ,t1.send_id as "sendId"
        ,t1.accept_id as "acceptId"
        ,t1.title as "title"
        ,t1.content as "content"
        ,t1.state as "state"
        ,t1.date_created as "dateCreated"
        ,t1.date_updated as "dateUpdated"
    </sql>

    <!-- 根据主键获取信息详情 -->
    <select id="selectByPrimaryKey" resultType="com.wu1g.vo.sys.SysMessage" parameterType="com.wu1g.vo.sys.SysMessage">
        select
        <include refid="Base_Column_List"/>
        from sys_message t1
        where
        t1.id = #{id}
    </select>

    <!-- 新增单条数据 -->
    <insert id="insert" parameterType="com.wu1g.vo.sys.SysMessage"  useGeneratedKeys="true" keyProperty="id"  >
        insert into sys_message(
         
            send_id 
           ,accept_id 
           ,title 
           ,content 
           ,state 
           ,date_created 
           ,date_updated 
        ) values (
         
            #{sendId} 
           ,#{acceptId} 
           ,#{title} 
           ,#{content} 
           ,#{state} 
           ,now()
           ,now()
        )
    </insert>


    <!-- 数据更新 -->
    <update id="update" parameterType="com.wu1g.vo.sys.SysMessage">
        update sys_message set date_updated=now() 
             <if test="sendId!=null">,send_id = #{sendId}</if>
             <if test="acceptId!=null">,accept_id = #{acceptId}</if>
             <if test="title!=null">,title = #{title}</if>
             <if test="content!=null">,content = #{content}</if>
             <if test="state!=null">,state = #{state}</if>
        where
        id = #{id}
    </update>

    <!--列表通用条件-->
    <sql id="list_where_sql">
        <if test="keyword!=null and keyword!=''">
            <![CDATA[ and t1.name like concat('%',#{keyword},'%') ]]>
        </if>
        <if test="dateBegin!=null and dateBegin!=''">
            <![CDATA[ and DATE_FORMAT(t1.date_created,'%Y-%m-%d')>=DATE_FORMAT(#{dateBegin},'%Y-%m-%d')]]>
        </if>
        <if test="dateEnd!=null and dateEnd!=''">
            <![CDATA[ and DATE_FORMAT(t1.date_created,'%Y-%m-%d')<=DATE_FORMAT(#{dateEnd},'%Y-%m-%d')]]>
        </if>
    </sql>

    <!--信息列表 分页 -->
    <select id="findDataIsPage" parameterType="com.wu1g.vo.sys.SysMessage" resultType="com.wu1g.vo.sys.SysMessage">
        select
        <include refid="Base_Column_List"/>
        from sys_message t1
        where 1=1
        <include refid="list_where_sql"/>
        order by t1.date_created desc
    </select>

    <!--信息列表 -->
    <select id="findDataIsList" parameterType="com.wu1g.vo.sys.SysMessage" resultType="com.wu1g.vo.sys.SysMessage">
        select
        <include refid="Base_Column_List"/>
        from sys_message t1
        where 1=1
        <include refid="list_where_sql"/>
        order by t1.date_created desc
    </select>
</mapper>