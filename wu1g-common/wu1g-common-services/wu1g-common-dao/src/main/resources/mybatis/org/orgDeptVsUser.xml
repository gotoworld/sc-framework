<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wu1g.dao.org.IOrgDeptVsUserDao">
    <!-- 新增 批量数据 -->
    <insert id="insertBatch">
        insert into org_dept_vs_user(
        user_id,
        dept_id,
        date_created
        ) values
        <foreach collection="list" item="t" separator=",">
            (
            #{t.userId},
            #{t.deptId},
            now()
            )
        </foreach>
    </insert>
    <!-- 根据用户id清空用户关联部门信息-->
    <delete id="deleteDataByUid" parameterType="orgDeptVsUser">
		delete
		from org_dept_vs_user
		where  user_id = #{userId}
	</delete>
</mapper>