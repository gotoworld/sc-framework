<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hsd.account.staff.dao.org.IOrgStaffAppDao">
    <!-- 字段列表 -->
    <sql id="Base_Column_List">
         t1.id as "id"
        ,t1.staff_id as "staffId"
        ,t1.app_id as "appId"
        ,t1.date_created as "dateCreated"
        ,t1.bi_update_ts as "biUpdateTs"
    </sql>

    <!-- 根据主键获取信息详情 -->
    <select id="selectByPrimaryKey" resultType="com.hsd.account.staff.entity.org.OrgStaffApp" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp">
        select
        <include refid="Base_Column_List"/>
        from org_staff_app t1
        where  t1.id = #{id}
    </select>

    <!-- 新增单条数据 -->
    <insert id="insert" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp"  >
        insert into org_staff_app (
            id 
           ,staff_id 
           ,app_id
           ,date_created 
           ,bi_update_ts 
        ) values (
            #{id} 
           ,#{staffId} 
           ,#{appId} 
           ,now()
           ,now()
        )
    </insert>


    <!-- 数据更新 -->
    <update id="update" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp">
        update org_staff_app set bi_update_ts=now()
        <if test="staffId!=null">,staff_id = #{staffId}</if>
        <if test="appId!=null">,app_id = #{appId}</if>
        where
        id = #{id}
    </update>

    <!--列表通用条件-->
    <sql id="list_where_sql">
        <if test="keyword!=null and keyword!=''">
            and t1.name like concat('%',#{keyword},'%')
        </if>
        <if test="dateBegin!=null and dateBegin!=''">
            <![CDATA[ and DATE_FORMAT(t1.date_created,'%Y-%m-%d')>=DATE_FORMAT(#{dateBegin},'%Y-%m-%d') ]]>
        </if>
        <if test="dateEnd!=null and dateEnd!=''">
            <![CDATA[ and DATE_FORMAT(t1.date_created,'%Y-%m-%d')<=DATE_FORMAT(#{dateEnd},'%Y-%m-%d') ]]>
        </if>
    </sql>

    <!--信息分页 -->
    <select id="findDataIsPage" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp" resultType="com.hsd.account.staff.entity.org.OrgStaffApp">
        select
        <include refid="Base_Column_List"/>
        from org_staff_app t1
        where 1=1
        <include refid="list_where_sql"/>
        order by t1.date_created desc
    </select>

    <!--信息列表 -->
    <select id="findDataIsList" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp" resultType="com.hsd.account.staff.entity.org.OrgStaffApp">
        select
        <include refid="Base_Column_List"/>
        from org_staff_app t1
        where 1=1
        <include refid="list_where_sql"/>
        order by t1.date_created desc
    </select>

    <select id="findDataByAppIdAndUserId" resultType="com.hsd.account.staff.entity.org.OrgStaffApp" parameterType="com.hsd.account.staff.entity.org.OrgStaffApp">
        select
         t1.id as "id"
        ,t1.staff_id as "staffId"
        ,t1.app_id as "appId"
        from org_staff_app t1
        where  t1.staff_id = #{staffId}
        AND  t1.app_id =#{appId}
    </select>
</mapper>